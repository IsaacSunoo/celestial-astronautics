import React from 'react';

const QuestionCard = (props) => {
  const { options, answerImage } = props;
  console.log(options)
  console.log('Question card entered');
  return (
    <div>
      {/* <h1>{options}</h1> */}
    </div>
  )
}

export default QuestionCard;